# name: SDK E2E Tests

# on:
#   schedule:
#     # Run nightly at 03:00 UTC
#     - cron: "0 3 * * *"
#   workflow_dispatch:
#     inputs:
#       contract_id:
#         description: "Contract ID to test against (optional)"
#         required: false
#         type: string

# jobs:
#   sdk-e2e:
#     name: SDK E2E Tests (Testnet)
#     runs-on: ubuntu-latest
#     timeout-minutes: 15

#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v4

#       - name: Set up pnpm
#         uses: pnpm/action-setup@v4

#       - name: Set up Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: 20
#           cache: pnpm

#       - name: Install dependencies
#         run: pnpm install --frozen-lockfile

#       - name: Run SDK E2E tests
#         working-directory: packages/sdk
#         env:
#           TESTNET_SECRET_KEY: ${{ secrets.TESTNET_SECRET_KEY }}
#           Kovara_CONTRACT_ID: ${{ github.event.inputs.contract_id || 'CDLDVFKHEZ2RVB3NG4UQA4VPD3TSHV6XMHXMHP2BSGCJ2IIWVTOHGDSG' }}
#         run: pnpm test:e2e

#       - name: Upload test results
#         uses: actions/upload-artifact@v4
#         if: always()
#         with:
#           name: sdk-e2e-results
#           path: packages/sdk/coverage/
#           retention-days: 7
