# name: Integration Tests

# on:
#   workflow_dispatch:
#     inputs:
#       run_e2e:
#         description: 'Run Playwright E2E tests after integration tests'
#         required: false
#         default: 'false'
#         type: choice
#         options:
#           - 'true'
#           - 'false'
#   schedule:
#     # Runs nightly at 02:00 UTC.
#     - cron: "0 2 * * *"

# jobs:
#   integration:
#     name: E2E Integration (sandbox)
#     runs-on: ubuntu-latest
#     # Kept separate from the unit-test workflow; requires Docker and stellar-cli.

#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v4

#       - name: Install Rust toolchain
#         uses: dtolnay/rust-toolchain@stable
#         with:
#           targets: wasm32-unknown-unknown

#       - name: Cache Rust build artefacts
#         uses: Swatinem/rust-cache@v2
#         with:
#           workspaces: packages/contracts

#       - name: Install stellar-cli
#         run: cargo install --locked stellar-cli --version 22.8.1

#       - name: Verify Docker is available
#         run: docker info

#       - name: Set up pnpm
#         uses: pnpm/action-setup@v4

#       - name: Set up Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: 24
#           cache: pnpm

#       - name: Install JS dependencies
#         run: pnpm install --frozen-lockfile

#       - name: Run integration tests
#         run: pnpm test:integration

#       - name: Install Playwright browsers (optional E2E)
#         if: github.event.inputs.run_e2e == 'true' || github.event_name == 'schedule'
#         run: pnpm exec playwright install --with-deps

#       - name: Run Playwright E2E tests (optional)
#         if: github.event.inputs.run_e2e == 'true' || github.event_name == 'schedule'
#         working-directory: packages/web
#         env:
#           CI: true
#         run: pnpm test:e2e

#       - name: Upload Playwright report (optional E2E)
#         uses: actions/upload-artifact@v4
#         if: (github.event.inputs.run_e2e == 'true' || github.event_name == 'schedule') && always()
#         with:
#           name: playwright-report
#           path: packages/web/playwright-report/
#           retention-days: 30
